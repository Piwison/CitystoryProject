# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Database URL for ORM
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres

# Optional PostgreSQL Settings
POSTGRES_MAX_CONNECTIONS=100
POSTGRES_IDLE_TIMEOUT=300
POSTGRES_STATEMENT_TIMEOUT=60000

# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Email Settings (if needed)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# AWS S3 Settings (for photo storage)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=your-region

# Redis Cache Settings (optional)
REDIS_URL=redis://localhost:6379/0

# API Rate Limiting
API_RATE_LIMIT=100/hour

# Moderation Settings
AUTO_APPROVE_TRUSTED_USERS=True
MODERATION_NOTIFICATION_EMAIL=moderator@example.com

# Note: This is an example configuration file.
# Copy these settings to a new '.env' file in your project root
# and replace the placeholder values with your actual configuration. 